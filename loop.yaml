- name: loop
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello, {{ item }}"
    loop:
    - Suresh
    - Mahesh
    - Jagan
    - Sagar
    - Avinash